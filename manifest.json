{
  "manifest_version": 2,

  "name": "Select -> Copy || Search",
  "description": "Shows popover with Copy and Search commands after text selection",
  "version": "1.0",

  "permissions": [
    "activeTab"
  ],
  "content_scripts": [
    {
      "matches": ["*://*/*"],
      "css": ["dist/style.css"],
      "js": ["dist/bundle.js"]
    }
  ]
}
